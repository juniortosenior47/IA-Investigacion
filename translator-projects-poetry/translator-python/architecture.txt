┌────────────────────────┐
│        Client          │
│  (Postman / curl / UI) │
└────────────┬───────────┘
             │ HTTP
             ▼
     ┌────────────────┐
     │   API Service   │
     │ (FastAPI / Node)│
     ├────────────────┤
     │ Endpoints:      │
     │ - /add_words    │
     │ - /translate    │
     │ - /translate_many│
     └────────┬────────┘
              │
              ▼
       ┌─────────────┐
       │    Redis     │
       │ (Cache KV)   │
       │ Key: translator:word:<term> │
       └─────────────┘
