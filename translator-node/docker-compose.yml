version: "3.9"
services:
  translator_node_app:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    depends_on:
      - redis
  redis:
    image: redis:latest
    ports:
      - "6379:6379"
